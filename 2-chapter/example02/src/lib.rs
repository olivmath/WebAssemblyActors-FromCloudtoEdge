// Todo
// add(i32, i32) -> i32
// sub(i32, i32) -> i32
// mul(i32, i32) -> i32
// div(i32, i32) -> i32

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
